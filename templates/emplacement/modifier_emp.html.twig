{% extends 'base.html.twig' %}

{% block title %}
<title>Edit Transpoteur</title>
{% endblock %}

{% block body %}

         
                




    <!-- row -->

    <div class="container">
        <div class="form-card1">
            <div class="form-card2">
                <div class="card">
                    <div class="card-header">
                        <h1 class="welcome-line-1">Emplacement</h1>
                    </div>
                    <div class="card-body">
                        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}

                        {{ form_row(form.superficie, {
                            'attr': {
                                'class': 'form-control'
                            }
                        }) }}

                        {{ form_row(form.disponibilite, {
                            'attr': {
                                'class': 'form-control'
                            }
                        }) }}

                        {{ form_row(form.centre, {
                            'attr': {
                                'class': 'form-control',
                            }
                        }) }}

                          <button type="submit" class="submit-button">Modifier</button>
                                <a href="{{ path('app_afficheremp')}}" class="submit-button">Annuler</a>

                                 <form action="{{ path('app_emp_edit', {'idemplacement': emplacement.getIdemplacement}) }}" method="POST" enctype="multipart/form-data">

                                {{ form_end(form) }}
                            </form>

                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="{{ asset('../vendor/global/global.min.js') }}"></script>
    <script src="{{ asset('./js/quixnav-init.js') }}"></script>
    <script src="{{ asset('./js/custom.min.js') }}"></script>
    <!-- Jquery Validation -->
    <script src="{{ asset('./vendor/jquery-validation/jquery.validate.min.js') }}"></script>
    <!-- Form validate init -->
    <script src="{{ asset('./js/plugins-init/jquery.validate-init.js') }}"></script>
        <div class="footer">
            <div class="copyright">
                <p>Copyright © Designed &amp; Developed by <a href="" target="_blank">Quixkit</a> 2019</p>
            </div>
        </div>
        
    </div>
    <script src=" {{asset('../vendor/global/global.min.js')}}"></script>
    <script src="{{asset('./js/quixnav-init.js')}}"></script>
    <script src="{{asset('./js/custom.min.js')}}"></script>
    <!-- Jquery Validation -->
    <script src="{{asset('./vendor/jquery-validation/jquery.validate.min.js')}}"></script>
    <!-- Form validate init -->
    <script src="{{asset('./js/plugins-init/jquery.validate-init.js')}}"></script>
    
{% endblock %}
