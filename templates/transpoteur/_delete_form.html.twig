<form method="post" action="{{ path('app_transpoteur_delete', {'num_ch': transpoteur.numCh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transpoteur.numCh) }}"> <!-- Changed transpoteur.id to transpoteur.numCh -->
    <button class="submit-button">Delete</button>
</form>
